# パッケージストアの整合性を検証する（改ざん検知）
verify-store-integrity=true

# TLS 証明書の検証を強制する（MITM 攻撃防止）
strict-ssl=true

# インストール時にセキュリティ監査を実行する
audit=true

# ファントム依存関係を防ぐ（pnpm デフォルト動作を明示化）
hoist=false

# 公開後 24 時間未満のパッケージのインストールを拒否する（poisoned package 攻撃対策）
minimumReleaseAge=4320
